@model SohaService.Core.DTOs.Expert.ExpertViewModel

@{
    ViewData["Title"] = "کارشناسان حذف شده";
}

<div class="row">
    <form>
        <div class="col-sm-5 col-md-5">
            <input type="text" name="filterFamily" class="form-control" placeholder="نام خانوادگی" />
        </div>
        <div class="col-sm-5 col-md-5">
            <input type="text" name="filterMobile" class="form-control" placeholder="موبایل" />
        </div>
        <div class="col-sm-2 col-md-2" style="margin: 10px 0 0">
            <button type="submit" class="btn btn-info">بگرد</button>
            <a class="btn btn-default btn-sm" href="/DeletedExperts">خالی</a>
        </div>
    </form>
</div>

<table class="table table-bordered table-condensed table-hover table-responsive table-striped text-center">
    <thead>
        <tr class="success">
            <th class="text-center">نام و نام خانوادگی</th>
            <th class="text-center">موبایل</th>
            <th class="text-center">دستورات</th>
        </tr>
    </thead>
    <tbody>

        @foreach (var item in Model.Experts)

        {
            <tr>
                <td>@item.ExpertName @item.ExpertFamily</td>
                <td>@item.ExpertMobile</td>
                <td>
                    <a href="/InformationExpert/@item.ExpertId" class="btn btn-primary btn-sm">
                        صفحه مشخصات
                    </a>
                </td>
            </tr>
        }

    </tbody>
</table>

<div class="row">
    <div class="col-sm-6">
        <div class="dataTables_paginate paging_simple_numbers" id="dataTables-example_paginate">
            <ul class="pagination">

                @for (int i = 1; i <= Model.PageCount; i++)
                {
                    <li class="paginate_button @((i == Model.CurrentPage) ? "active" : "")" aria-controls="dataTables-example" tabindex="0">
                        <a href="/DeletedExperts?PageId=@i">@i</a>
                    </li>
                }

            </ul>
        </div>
    </div>
</div>

